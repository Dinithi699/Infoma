<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryDark">@color/primary_dark_color</item>
        <item name="colorAccent">@color/accent_color</item>
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@color/status_bar_color</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- NoActionBar Theme -->
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- AppBar Overlay -->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <!-- Popup Overlay -->
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Text Styles -->
    <style name="TextAppearance.Title" parent="TextAppearance.AppCompat.Title">
        <item name="android:textSize">@dimen/text_size_large</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="TextAppearance.Subtitle" parent="TextAppearance.AppCompat.Subhead">
        <item name="android:textSize">@dimen/text_size_medium</item>
        <item name="android:textColor">@color/text_secondary</item>
    </style>

    <style name="TextAppearance.Body" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textSize">@dimen/text_size_normal</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="TextAppearance.Caption" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textSize">@dimen/text_size_small</item>
        <item name="android:textColor">@color/text_tertiary</item>
    </style>

    <!-- Card Styles -->
    <style name="CardView.News" parent="CardView">
        <item name="cardCornerRadius">@dimen/card_corner_radius</item>
        <item name="cardElevation">@dimen/card_elevation</item>
        <item name="cardBackgroundColor">@color/card_background</item>
        <item name="android:layout_margin">@dimen/card_margin</item>
    </style>

    <!-- Button Styles -->
    <style name="Button.Primary" parent="Widget.AppCompat.Button.Colored">
        <item name="android:background">@color/button_primary</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/text_size_normal</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingLeft">@dimen/button_padding_horizontal</item>
        <item name="android:paddingRight">@dimen/button_padding_horizontal</item>
        <item name="android:paddingTop">@dimen/button_padding_vertical</item>
        <item name="android:paddingBottom">@dimen/button_padding_vertical</item>
        <item name="android:minHeight">@dimen/button_height</item>
    </style>

    <style name="Button.Secondary" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/button_secondary</item>
        <item name="android:textSize">@dimen/text_size_normal</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingLeft">@dimen/button_padding_horizontal</item>
        <item name="android:paddingRight">@dimen/button_padding_horizontal</item>
        <item name="android:paddingTop">@dimen/button_padding_vertical</item>
        <item name="android:paddingBottom">@dimen/button_padding_vertical</item>
        <item name="android:minHeight">@dimen/button_height</item>
    </style>

    <!-- Toolbar Styles -->
    <style name="Toolbar.Title" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">@dimen/text_size_large</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Navigation Styles -->
    <style name="NavigationView" parent="Widget.Design.NavigationView">
        <item name="android:background">@color/drawer_background</item>
        <item name="itemTextColor">@drawable/drawer_item_color</item>
        <item name="itemIconTint">@drawable/drawer_item_color</item>
    </style>

    <!-- Category Tag Style -->
    <style name="CategoryTag">
        <item name="android:background">@drawable/category_bg</item>
        <item name="android:textColor">@color/category_text</item>
        <item name="android:textSize">@dimen/text_size_small</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingLeft">@dimen/category_padding_horizontal</item>
        <item name="android:paddingRight">@dimen/category_padding_horizontal</item>
        <item name="android:paddingTop">@dimen/category_padding_vertical</item>
        <item name="android:paddingBottom">@dimen/category_padding_vertical</item>
    </style>

    <!-- Fragment Container Style -->
    <style name="FragmentContainer">
        <item name="android:background">@color/fragment_background</item>
        <item name="android:padding">@dimen/fragment_padding</item>
    </style>

    <!-- News Item Styles -->
    <style name="NewsTitle" parent="TextAppearance.Title">
        <item name="android:maxLines">2</item>
        <item name="android:ellipsize">end</item>
        <item name="android:lineSpacingExtra">@dimen/line_spacing_extra</item>
    </style>

    <style name="NewsDescription" parent="TextAppearance.Body">
        <item name="android:textColor">@color/text_secondary</item>
        <item name="android:maxLines">2</item>
        <item name="android:ellipsize">end</item>
        <item name="android:lineSpacingExtra">@dimen/line_spacing_extra</item>
    </style>

    <!-- Profile Image Style -->
    <style name="ProfileImage">
        <item name="android:layout_width">@dimen/profile_image_size</item>
        <item name="android:layout_height">@dimen/profile_image_size</item>
        <item name="android:background">@drawable/profile_circle_bg</item>
        <item name="android:scaleType">centerCrop</item>
    </style>

</resources>